SMART AUTH — структура таблиц аутентификации
=============================
ТАБЛИЦА: smart_users
=============================
📌 Назначение

Хранение информации о пользователе, включая email, имя, пароли (в открытом и хэшированном виде), уровень доступа и дату регистрации.

📄 Структура таблицы
Поле	Тип данных	NULL	По умолчанию	Описание
id	uuid	NO	gen_random_uuid()	Уникальный ID пользователя (PRIMARY KEY)
email	text	NO	—	Email, используется как логин, UNIQUE
name	text	YES	NULL	Имя пользователя, как он представился
password	text	YES	NULL	Пароль в открытом виде (как договорено)
password_hash	text	YES	NULL	Хэш пароля (bcrypt)
level	integer	YES	2	Уровень доступа (1 = гость, 2 = обычный юзер)
role	text	YES	'user'	Тип роли (например, user / admin)
created_at	timestamp	YES	now()	Дата регистрации
updated_at	timestamp	YES	now()	Последнее обновление записи
🔍 Особенности таблицы

level = 2 по умолчанию → пользователь сразу получает «юзер-меню».

Пароли хранятся двумя способами:

password — открытый пароль (для удобства на старте разработки)

password_hash — безопасный хэш (для реального входа)

Все дополнительные поля допускают NULL, чтобы запись никогда не ломалась.

Таблица легко расширяется при необходимости.

=============================
ТАБЛИЦА: smart_sessions
=============================
📌 Назначение

Хранение токенов сессий пользователей.
Каждый токен соответствует конкретному человеку и используется через cookie.

📄 Структура таблицы
Поле	Тип данных	NULL	По умолчанию	Описание
id	uuid	NO	gen_random_uuid()	ID сессии (PRIMARY KEY)
user_id	uuid	YES	—	Связь с smart_users(id), привязка токена к пользователю
token	text	NO	—	Уникальный токен, кладётся в cookie
expires_at	timestamp	NO	—	Когда сессия протухает
created_at	timestamp	YES	now()	Когда сессия была создана
last_used_at	timestamp	YES	now()	Последняя активность пользователя
user_agent	text	YES	NULL	Информация об устройстве / браузере (короткая строка)
🔐 Индексы таблицы
Имя индекса	Описание
smart_sessions_pkey	PRIMARY KEY по полю id
smart_sessions_token_key	UNIQUE индекс по token
🔍 Особенности таблицы

token всегда уникален → позволяет безопасно находить сессию.

last_used_at помогает чистить старые мёртвые токены.

user_agent используется для базовой аналитики (допускает NULL).

Таблица абсолютно безопасная — нет IP, нет чувствительных данных.